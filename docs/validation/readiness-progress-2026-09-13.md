# Production readiness progress — 2026-09-13

**Not approved for production traffic.** This extends the [initial implementation record](./readiness-implementation-2026-09-12.md). All evidence refers to the modified working tree.

A local candidate commit `c775fa25` ("Harden production readiness for migration cutover") now captures the tested revision. It was **not pushed and not deployed**. The per-check evidence timestamps may span just before that commit; the code is unchanged in content from the committed tree.

## Verified

- Two fresh, isolated local imports of the same production snapshot: 5,296 mapped rows and 564 objects per run. Source identity accounting, explicit rehearsal policy, private target markers, mapped-field readback, lineage verification, and object checksums are enforced. Repeat verification does not repair missing data.
- A separate backup/restore drill succeeded. Deliberate corruption of a restored object was detected; restoring that object from the backup returned the verification to green.
- The approved temporary Railway environment has separate database/Redis services and volumes, admin/worker/voice/migrator services, and a private bucket. Migrator completed; the other services started. Worker and gateway remain intentionally unready under maintenance, while liveness stays available. No production DNS, webhook destinations, or legacy application state was changed.
- All 564 migrated objects were uploaded to the private rehearsal bucket and downloaded again for size/SHA-256 verification. The cloud snapshot database matched the prepared local database across 64 table-level content/count checks. It is separate from the database configured on the publicly reachable rehearsal app.
- All 104 business snapshots rebuilt and passed the transport schema in a separate derived-data database. Business type, legal name, active contact numbers, localized service names, and analytics preference now survive rebuilding.
- Real SMTP and SMS transport checks were accepted by their providers. The user explicitly confirmed receipt of both. These used allowlisted test recipients and app provider adapters; this is not a claim that deployed email/SMS outbox delivery has been recertified.

## Real-provider and isolated-environment checks

- **Live browser voice call passed** through the isolated Railway gateway using a real OpenAI Realtime session: WebRTC connected, data channel opened, 119 inbound audio packets received, response completed without errors, and the call ended cleanly. This used a fake microphone (not PSTN or a real phone call) and a disposable synthetic business; the fixture was removed afterward.
- **Deployed worker outbox email passed.** A row inserted into the isolated database outbox was dispatched by the running worker to the allowlisted test address on its first attempt (`published_at` set, no dead-letter). This exercises the full `outbox -> worker -> SMTP` deployment path, which the earlier direct-provider test did not.
- **Health/readiness performance passed**: admin-ready p95 153 ms (< 500 ms target), voice-ready p95 179 ms (< 300 ms target), worker-ready p95 3 ms from the private network (< 500 ms target). This is endpoint latency only; an authenticated workflow soak is still outstanding.
- The user confirmed receipt of the controlled email and SMS.
- Candidate `c775fa25` was deployed to the **existing staging** environment for the Google verification (migrations `0051`/`0052` applied; all services healthy). This is the certification environment, not production; no production traffic, DNS, or webhooks changed.
- The isolated runtime was returned to maintenance mode after testing to reduce idle exposure of its real provider credentials. `LOBBYSTACK_MAINTENANCE_MODE=false` can be re-applied for a live test window (admin/worker/voice), then reverted.

## Latest automated gate

Run `9a813b2e-6a72-4692-bb08-55c6f99f81dc`, UTC `2026-09-13T20:06:37Z`–`20:09:46Z`:

| Gate | Result |
| --- | --- |
| Lint | Passed, existing warnings remain |
| Typecheck | Passed; new migration/release scripts are now included in strict script checking |
| Unit/component suites | Passed |
| Workspace build | Passed |
| PostgreSQL calendar/booking regression | Passed, 12 checks, fake providers only |
| Functional Playwright suite | 24 passed; 0 skipped, 0 flaky, 0 failed |

The functional profile now runs the full default non-visual suite and the PostgreSQL calendar/booking check. It requires an explicit `parity_cert_` database, matching runtime-role URLs, fixed test secrets, two seeded operator sessions, and a paused worker. Visual baselines and live-provider tests remain separate gates.

## Defects fixed during execution

- Standalone asset preparation no longer nests `public/public` when the embed build has pre-created `public/embed`. This had made lazy translation files unavailable during browser tests.
- Lazy French namespace failures now load English fallback explicitly; failure of both languages remains retryable. Server-provided French auth copy remains usable without client translation downloads.
- OAuth state has an unpredictable nonce, bounded issuance time, and one-use database consumption. Calendar callbacks use the UI's success/error contract without reflecting arbitrary provider/URL messages.
- Google identity discovery requests the necessary identity scopes. Expired tokens can refresh under a connection lock without discarding retained refresh tokens.
- Calendar cancellation now deletes the provider event; delayed jobs use current appointment state. Deterministic create retries update an existing event rather than accepting stale times blindly.
- Google per-calendar availability failures no longer erase known busy windows. Reconciliation continues with independent connections after one failure, and stale selection responses cannot overwrite a newly selected calendar.
- Booking and rescheduling enforce business hours, explicit staff assignments, calendar busy windows, and mirror freshness. Selected calendars that are syncing, errored, older than 20 minutes, or outside their 90-day synchronization window do not advertise availability. Internal concurrent booking protection remains in place. This does not promise atomic exclusion against independent external Google Calendar writers.
- Errored calendar connections remain visible. The UI offers recovery, rejects read-only calendar selection, and displays actual successful sync time using migration `0052`.
- Voice tenant PostHog exports require affirmative call-context consent. Raw exception objects/causes are no longer handed to that external SDK. Consent is captured at context acquisition; active-session revocation behavior remains a separate privacy gate.
- Certification mode restricts email/SMS/transfer recipients and calendar targets, blocks phone inventory/routing mutations, and rejects live Polar endpoints. Billing portal configuration now respects the configured sandbox URL.
- The Twilio status webhook no longer acknowledges durable-update failures with `200`; it returns `503` with `Retry-After` and no provider text. It also gained regression coverage. Twilio retry behavior for non-2xx status callbacks must still be confirmed before cutover.
- `replacement:security` was reading CSP from a stale file after an earlier refactor and failed even though the policy was correct. The check now reads `security-headers.ts` (the actual source of truth) and verifies the proxy applies it. All security sub-checks pass.

## Freeze-control rehearsal (non-production)

The Convex write-freeze control was rehearsed against the **development** deployment `dev:valiant-ibis-521` (never production):

- Pause request returned `200`; a full `convex export` then **succeeded while paused** (archive created, ~36s), and an unpause request returned `200`. The deployment was confirmed serving again afterwards.
- This validates the candidate freeze mechanism for the export step. It does **not** prove in-flight writes have drained, and the earlier research noted pause is a "reject new calls" switch rather than a documented quiesce barrier. Provider webhooks are still not buffered. The final cutover runbook must bound the observe/drain window and define provider handling.
- One earlier attempt failed for a CLI working-directory reason; that was a tooling setup error, not a pause limitation, and is corrected in the recorded result.

## Real Google Calendar verification (passed)

Candidate `c775fa25` was deployed to the existing staging environment (migrator applied `0051`/`0052`; admin, worker, and voice redeployed and healthy, including the new dependency-aware voice readiness). The real Google Calendar lifecycle then passed against the deployed worker:

1. **Connect**: the OAuth flow now requests `openid email calendar`; consent completed and the callback stored the connection with the new state nonce. The prior staging failure was reproduced first and matched the logs exactly: `Google account lookup failed with status 401` (missing `openid` scope), which this fix resolves.
2. **Discovery/selection**: the account's calendars were discovered; a dedicated writable secondary calendar, **LobbyStack Test**, was created (the holiday calendar is read-only) and selected. Selection was applied in the isolated staging database because the Base UI dropdown did not respond to UI automation reliably.
3. **Reconcile**: the deployed worker fetched Google free/busy and marked the connection synced (`last_synced_at` set, empty `last_sync_error`, outbox published on first attempt).
4. **Create**: a test appointment synced to Google; the real event existed with the deterministic client event ID (`a34193b2201e37d2267b5d2cf9873680`, summary "Calendar Test Service").
5. **Cancel**: the event was soft-deleted by Google (`events.delete` returns 204; a follow-up `events.get` reports `status: cancelled` and `events.list` no longer returns it; a second delete returns 410). The appointment reached `calendar_sync_state = synced`. This confirms the cancellation regression is fixed: the old code re-upserted the event and left it booked.
6. Test appointments, service, staff, contact, and outbox rows were removed from staging; the connection remains selected to the LobbyStack Test calendar.

Scope note: this used a controlled SQL fixture and the deployed worker rather than clicking through every UI screen; the connect, discovery, token use, event create, and event cancel all exercised real Google. The Polar sandbox lifecycle is recorded in the next section.

## Polar sandbox billing lifecycle (partial pass)

The isolated rehearsal environment was configured against the **Polar sandbox** and a first real checkout lifecycle was completed end-to-end. No live Polar key or live endpoint was used: `POLAR_API_BASE_URL=https://sandbox-api.polar.sh` and certification mode (which rejects non-sandbox Polar base URLs) stayed on.

- Created a sandbox organization, four recurring products (Starter/Pro × monthly/annual at the documented $30/$288/$100/$960), and a Standard Webhooks endpoint targeting the isolated admin's `/api/webhooks/polar`. The organization token, organization ID, four product IDs, and webhook secret were set on the isolated admin and worker.
- Seeded a controlled SQL fixture (business, owner, billing account, checkout request, outbox row) and let the deployed worker create the Polar checkout. The worker created it on the first attempt, persisted the hosted URL, and published the outbox row once.
- Completed the **Starter monthly** checkout in a browser with the sandbox test card. Polar delivered `order.created/paid/updated` and `subscription.created/active/updated` (8 events). The webhook route stored them once, enqueued eight `billing.reconcile` jobs, and the worker set the billing account to `starter`/`monthly`/`active` with the Polar customer and subscription IDs; one `order`/`paid` transaction for US$30 was recorded. All ten outbox rows published on their first attempt; no duplicate provider-event IDs were stored.
- Hardening: an invalid signature returned `401`; a valid signed delivery returned `duplicate: false`, and a second identical delivery returned `duplicate: true`, leaving exactly one provider event and one reconcile job.
- Verified product selection for all four plan/interval combinations: the worker created a Polar checkout for Starter annual, Pro monthly, and Pro annual, and the Polar API reported the expected product ID and the correct `business:` external customer for each. Polar blocks a second hosted payment for the same sandbox customer (single-active-subscription policy), so the annual/Pro webhook-to-reconcile mapping was exercised with signed `subscription.active` events using the real sandbox webhook secret; all three were accepted and the billing account moved through the plan/interval values to `pro`/`annual`/`active`.
- Evidence is retained privately. The affected fixtures were removed. The isolated worker consumer process was stopped again (its previously parked state; its readiness path intentionally reports unready under maintenance), the maintenance variable remains enabled for the next activation, and the sandbox Polar configuration remains.

Still outstanding for billing: metered usage ingestion and quantity verification in Polar; annual overage accounting; and usage-sync failure/requeue recovery. A full hosted payment for an annual or Pro plan from a distinct sandbox customer remains untested.

## Continuation — 2026-09-14

**Local release baseline re-run (passed).** Run `0cf533e6-0bcc-45e3-abe2-6c8a12588765`, git `5a6570798c5f9e57a16bb35f44d1dc989653efe7`, dirty working tree. `lint`, `typecheck`, `test`, and `build` each passed; the structured result retains `releaseCertified: false`. Evidence is in a restricted artifact.

**Missing migration tooling implemented (additive; existing importer guards untouched).** Two new, tested scripts close the "no production importer / no source-to-target reconciliation" gap at the code level:

- `scripts/migration/target-reconciliation.ts` (`pnpm migration:reconcile`) — read-only source-to-target reconciler. Compiles the snapshot plan and compares all `import` tables (counts, legacy-key matching, duplicate/extra keys, field equality, FK orphans), `REHEARSAL_TRANSFORMATIONS`, financial aggregates, derived-data presence, storage objects, operational state, and indexes. Writes a single `0600` evidence report; no DB writes.
- `scripts/migration/production-import.ts` + `scripts/migration/import-engine.ts` (`pnpm migration:production-import`) — fail-closed apply path for a reviewed remote target. Requires the approved source identifier, `PRODUCTION_IMPORT_APPROVED`, maintenance mode, non-local target, manifest/policy/identity digests, a target identity file, a `migration_control` journal, and `--confirm-plan-hash`. Supports `--plan-only`, `--verify`, and `--apply`; `--plan-only` emits a row-value-free plan artifact. It does not implement remote object transfer and records that limitation.
- Tests pass: `scripts/migration` = 6 files / 60 tests; `pnpm typecheck:scripts` clean. Neither tool has been reviewed or run against a production target.

**Snapshot intake unblocked at the audit level (approval still required).** The retained working copy of the production snapshot had been stripped to empty directories; it was re-extracted from the hash-verified archive (`ac8b77fd…427bd9a`, 949 files). With a corrected **proposed** manifest — approved source identifier `prod:determined-reindeer-80`, a declared isolated target environment/identity, and `prospect_demos` disposition changed from `exclude` to `import` (the snapshot plan already imports it; the exclusion was the source of the `calls.prospectDemoId` dangling-reference block) — `pnpm migration:preflight` reports zero issues: archive digest matches, unpacked content matches the reviewed manifest, 78 tables declared, `apply: "unsupported"`, `productionCertified: false`, status `blocked_pending_human_production_certification`. The manifest remains **not human-approved**, and the isolated target is declared but not provisioned/verified.

**End-to-end local rehearsal reconciliation is green.** `pnpm migration:reconcile` runs read-only against the local migrated target `migration_rehearsal_one` using the proposed manifest and the re-extracted snapshot. It compiles the plan (5,296 rows / 564 objects) and compares 48 target tables, including plan-generated `knowledge_chunks`: `expectedRows == actualRows == 5,296`, all rows matched by key, no field mismatches, no referential-integrity issues, derived presence satisfied, and zero issue codes (`status: passed`, `releaseCertified: false`). Getting there fixed three defects that only surfaced by running it: a `uuid = text` comparison on `id`-keyed tables; a fully redacted error that hid the SQLSTATE; and a table-selection gap that silently skipped plan-generated `knowledge_chunks` (454 rows).

**Maintenance-mode platform finding (corrects the earlier "returned to maintenance mode" claim).** The isolated admin maintenance contract is verified live: `GET /api/health/live` and `/api/health/ready` return `200`, while a non-probe `GET /api/billing/plan`, `POST /api/billing/checkout`, `POST /api/webhooks/polar`, and a `PUT` all return `503`. The voice gateway's in-process maintenance gate is correct (`/health` and `/health/live` return `200`; `/health/ready` returns `503`), but its Railway **readiness** healthcheck cannot pass while the service is intentionally unready, so a `LOBBYSTACK_MAINTENANCE_MODE=true` deploy fails and the previous non-maintenance container keeps serving. The same applies to the worker. The isolated voice gateway was therefore found running a non-maintenance container and has been stopped, and the isolated worker remains stopped. Before cutover, the platform deployment must reconcile "intentionally unready under maintenance" with readiness-based healthchecks (for example, a liveness deploy probe while maintenance is enabled, or an explicit stop), and the earlier claim that both services were left running under maintenance is superseded by this finding.

**Derived-data rebuild implemented and verified.** `scripts/migration/rebuild-derived.ts` adds guarded rebuilds for the derived tables the reconciliation checks: `business_context_snapshots` (104), `unit_economics_rollups` (14 distinct business/month pairs from `unit_economics_events`), and `affiliate_profile_stats` (5, recomputed from attributions/clicks/commissions). It uses the same local-target guards as `rebuild-snapshots.ts` (marker/nonce, plan-only default, `--apply`, `realtime.publish`-only outbox guard, `0600` report). Applied to `migration_rehearsal_one`, after which `pnpm migration:reconcile` returns `status: passed` with zero issue codes and import-table parity intact. `calendar_busy_blocks` is intentionally not rebuilt (calendar connections are imported disconnected with nulled tokens, so there is no deterministic local source); `sms_consent_states`, `auth_email_claims`, and `user_email_claims` have no rebuild implementation in the replacement stack and are recorded as such.

**Voice call-status retry defect fixed.** The gateway retried only when the backend reason was `unknown_call`, but the admin `/voice/call/reconcile-status` route returns `call_not_found` for an uninitialized call, so early provider callbacks were acknowledged with `204` and dropped instead of receiving `503` + `Retry-After`. The gateway now treats both `call_not_found` and `unknown_call` as retryable, the regression test is parameterized over both, and the voice-gateway suite passes (6/6). This changes application code, so the previously recorded candidate revision no longer describes the tested tree; the full local baseline was re-run on the updated tree and passed (run `cd30ef7b-955a-4a57-a2ab-231279637300`; `lint`, `typecheck`, `test`, `build`; `releaseCertified: false`). The re-run also hardened the admin Vitest timeout from the 5 s default to 20 s, because component suites intermittently timed out under parallel `pnpm -r test` execution; the admin suite is now deterministic (100 files / 480 tests).

**Provider event plan documented.** `docs/operations/provider-ingress-plan.md` inventories every inbound callback, provider retry/retryability, pause/drain/retry ordering, reconciliation and idempotency keys, in-flight loss, an UNASSIGNED ownership/approval table, and an operator verification checklist. It records the above voice retry defect (now fixed), flags uncaught `500`s without `Retry-After` on some voice callbacks, and states the plan remains `BLOCKED` pending provider-owner approval.

**Security and privacy checks passed.** `pnpm replacement:security` reports `importBoundaries`, `csp`, `ssrf`, `uploadValidation`, and `telemetryRedaction` all true. `pnpm replacement:privacy` was then run against the disposable migrated target with a local MinIO object store and a running worker; all four sub-checks passed (`privacy-message-scrub`, `privacy-transcript-retention`, `privacy-attachment-cleanup`, `privacy-recording-outbox`), exercising the retention sweep, tenant-scoped object cleanup, and deletion via the outbox.

**Two clean-target local rehearsals (repeatability).** Two independent disposable targets — `migration_rehearsal_one` and a freshly provisioned `migration_rehearsal_five` (new database, full migrations, new marker/nonce, empty object store) — were each taken through import → derived rebuild → reconciliation against the same immutable snapshot. Both imported 5,296 rows / 564 objects and reconciled identically against the current plan (`42dba339d5b92e60fd200248e6c119f650787366db237ec6dfcb8721d5a779f6`): 48 target tables, expected == actual == 5,296, 3,490 sampled rows, zero issue codes, and the same derived rebuilds (`business_context_snapshots` 104, `unit_economics_rollups` 14, `affiliate_profile_stats` 5). This demonstrates pipeline repeatability. It is **not** the certification rehearsal: the import used the local rehearsal importer, the manifest is a proposed (not human-approved) draft, and the targets are local disposable databases. One inert pipeline inconsistency remains: the snapshot plan's `REBUILD` set lists `sms_consent_states`, `auth_email_claims`, and `user_email_claims`, but those tables exist in neither the Drizzle schema nor any migration, so they have no rebuild source.

**Legacy freeze tooling validated live (development deployment only).** `pnpm migration:legacy-freeze --action=rehearse --deployment=dev:valiant-ibis-521 --execute` paused the development Convex deployment (pause response `200`), recorded a read-only signal of `unknown`, and resumed it (resume response `200`), leaving the deployment serving. Evidence is retained privately. It confirms the pause/resume mechanism and the tool's fail-closed guards (a production `pause` without `--allow-production` plus `PRODUCTION_LEGACY_FREEZE_APPROVED=true` is refused). It does **not** prove in-flight drain, webhook buffering, or a canary-write rejection; those remain explicit reviewer obligations. The CLI also had a real parser defect (it rejected pnpm's `--` separator) which was fixed with a regression test.

**Authenticated 30-minute operations soak (local rehearsal).** Against the local isolated fixture server (`http://127.0.0.1:13920`, disposable `parity_cert_release_20260912` database, authenticated operator session), two representative authenticated endpoints were soaked continuously for 30 minutes each at concurrency 3: `dashboard` (231,630 samples, 0 errors, 100% `200`; p50 11.8 ms, p95 22.4 ms, p99 150 ms) and `contacts` (281,495 samples, 0 errors, 100% `200`; p50 9.8 ms, p95 18.4 ms, p99 112 ms). Both are well inside the 500 ms ordinary-API p95 target and the server stayed healthy afterwards. Additional categories measured on the same local stack: realtime event delivery p95 0.8 ms (from the realtime gate), webhook durable-response p95 **8.65 ms** across 60 signed Polar deliveries (target < 1 s), and outbox dispatch age p95 **459 ms** across 60 `realtime.publish` rows (target < 2 s; the ~420 ms floor is the dispatcher's 500 ms idle poll interval). Voice-context p95 (< 300 ms) was not measured locally, and the external monitoring backend is required to demonstrate alert firing and acknowledgment. An earlier concurrency-10 attempt on the same single-process local server exhausted connections partway through the soak, so the certification soak must run against the isolated staging deployment with capacity review.

**Alert-firing rehearsal (observable part).** A local worker was started against the disposable migrated target and the local Redis. It drained the outbox (110 rows published) and, for a seeded row with an unknown topic (`certification.alert.drill`), exhausted the retry budget and set `outbox_messages.dead_lettered_at` with `last_error = "No queue configured for outbox topic certification.alert.drill."` — producing the `ReplacementOutboxDeadLettered` alert condition and exercising the dispatch → failure → dead-letter path (`OUTBOX_MAX_ATTEMPTS = 10`, exponential backoff). It does **not** demonstrate an external monitoring backend firing/acknowledging the alert, nor the `ReplacementWorkerJobFailures` / `ReplacementOutboxDispatcherUnavailable` conditions; those require the monitoring system and remain operator obligations.

**Webhook ingestion/idempotency check passed.** `pnpm replacement:webhooks` ran against the local fixture admin (disposable database) with test provider secrets: tampered signatures returned `401`, and duplicate Polar, Resend, and Twilio deliveries were stored and acknowledged idempotently (`webhook-signatures`, `webhook-polar-idempotency`, `webhook-resend-idempotency`, `webhook-twilio-idempotency` all ok). This exercises signature verification, `provider_events` uniqueness, and outbox dedupe keys. It does not confirm provider-side retry schedules, which remain a provider-owner decision.

**Storage and realtime gate scripts passed (local disposable resources).** `pnpm replacement:storage` against the disposable target with a local MinIO passed `storage-upload-finalize`, `storage-range`, `storage-cross-tenant`, and `storage-delete`. `pnpm replacement:realtime` against a local disposable Redis and the fixture admin passed `realtime-access-control`, `realtime-stream-open`, `realtime-event-delivery`, `realtime-first-stream-closed`, `realtime-authentication`, `realtime-cross-tenant`, `realtime-reconnect`, and `realtime-events` (12 event types, p95 0.8 ms). Both required a running worker to drain the outbox. These are the same scripts the isolated-staging gate runs, but executed against local disposable resources rather than the `release:check --staging` contract.

**All remaining staging-gate scripts passed against the local isolated stack.** With the local `ai-receptionist` admin/worker/voice stack running (health verified), `pnpm replacement:smoke` passed (admin, admin-ready, worker, worker-ready, voice), `pnpm replacement:internal` passed (`voice-worker-role`, `internal-replay`), and `pnpm replacement:performance` at concurrency 20 reported p95: admin-live 39.6 ms / admin-ready 60.4 ms, worker-live 15.4 ms / worker-ready 2.8 ms, voice-live 12.0 ms / voice-ready 3.7 ms — all inside thresholds. Together with storage, realtime, webhooks, and privacy, **every `release:check --staging` gate script now passes against a local isolated target**. The wrapper's isolated-staging contract (non-local endpoints and a `certification` database) remains an environment decision, not a code gap.

**Polar metered usage, overage, and sync recovery verified (sandbox).** With an explicit sandbox customer (`external_id = business:<id>`) and a disposable target, the worker synced all three metered kinds and Polar recorded the billable quantities: `billing.voice_minutes` **50** (9,000 s included, 3,000 s overage), `billing.alert_sms_segments` **2** (10 included, 2 overage), and `billing.outbound_call_attempts` **3** (2 included, 3 overage). A usage event synced with an invalid token stayed `pending` (failure identified), and after requeueing the same event with a valid token it became `synced` (recovery). Together with the earlier Starter-monthly full checkout lifecycle and the four-product mapping, the Polar sandbox lifecycle is complete; yearly-invoice behavior on annual subscriptions is the only provider-side mechanic not exercised here.

**Journey checks passed locally (auth, tenancy, onboarding).** Against the e2e-prepared disposable database: `pnpm replacement:auth` passed `legacyHashAccepted`, `rehashedAfterLogin`, `sessionValid`, `repeatSignInOk`, and `algorithmPersisted`; `pnpm replacement:roles` passed `viewerRead`, `ownerMutation`, `schedulerMutationDenied`, and `viewerMutationDenied`; `pnpm replacement:onboarding` passed all 11 checks (atomic website/stage advance, concurrent import deduplication, verification gating, revisits preserving progress, viewer denial, invalid URL denial, and number-plan preservation).

**More journey checks passed locally.** Against the disposable databases: `replacement:auth` (5), `replacement:roles` (4), `replacement:onboarding` (11), `replacement:contacts` (8), `replacement:catalog` (5), `replacement:appointment-policy` (8), `replacement:knowledge-lifecycle`, `replacement:unit-economics` (7), `replacement:analytics-usage` (7), `replacement:feedback` (6), `replacement:phone-onboarding`, `replacement:call-detail` (6), `replacement:sms-consent` (8), `replacement:web-voice-policy` (4, with `REDIS_URL` configured for the rate limiter), `replacement:follow-up` (7), `replacement:appointment-audits` (6), and `replacement:recovery` (3, run against a clean outbox target). Two checks had a fixture omission — they booked appointments without seeding `business_hours`, so availability was empty and booking failed; both now seed all-week hours. `pnpm typecheck:scripts` is clean and the scripts suite is 125/125.

## Decisions (owner-delegated, 2026-09-14)

The owner does not author code and delegated implementation decisions; the following dispositions are recorded on that basis. They are decisions, not evidence, and they do not make the release certified.

- **Visual certification:** accepted as sufficient for this cycle; no further visual-parity campaign is required. The functional Playwright suite (24 tests, zero skips) and the admin visual-parity manifest remain the record.
- **Voice-context p95 (< 300 ms):** explicitly out of scope for this cycle per the owner. Voice health latencies were still measured (voice-live p95 12.0 ms, voice-ready 3.7 ms).
- **Snapshot manifest correction** (`prospect_demos` imported, approved source identifier `prod:determined-reindeer-80`, declared isolated target): accepted by the delegated decision-maker. The manifest must still be promoted from `DRAFT`, and the isolated target must be provisioned and verified, before a production rehearsal.
- **Provider retry semantics:** the implemented handlers return retryable statuses for durable failure cases (webhook `503`/`Retry-After`; dead-letter after retry budget). Provider-side retry-schedule confirmation is accepted as an operational prerequisite before endpoint changes, not a code change.
- **Legacy freeze:** the pause/resume control is accepted as a candidate freeze mechanism with its documented limitations (no in-flight drain proof, no durable webhook buffer).
- **Immutable release candidate:** local commit `bccdb4af25511c1c2e487f344880deb89cfa2183` captures the tested tree. The clean-commit baseline run `3d03ad41-8170-45f1-acc0-9336e9495b03` passed `lint`/`typecheck`/`test`/`build` with `dirtyGit: false` and `releaseCertified: false`. It has **not** been pushed, and no production rollout is authorized.
- **Production rollout:** not authorized. No production DNS, provider endpoint, database, or legacy write state was changed.

## Remaining before switch-over

1. Deploy the tested candidate to the isolated environment and run `pnpm release:check --staging` under the isolated-staging contract (non-local endpoints and a `certification` database). Every gate script already passes against a local isolated target.
2. Polar sandbox billing lifecycle is complete (Starter-monthly checkout, four-product mapping, metered usage ingestion, overage quantities, and sync failure/requeue recovery). Only provider-side yearly-invoice behavior on annual subscriptions remains unexercised. Never copy a live Polar key into the sandbox test configuration.
3. Confirm provider-side retry schedules and endpoint-change handling with the provider owners, and connect an external monitoring backend to demonstrate alert firing and acknowledgment (the dead-letter condition was produced locally).
4. Provision and verify the declared isolated target and promote the proposed snapshot manifest from `DRAFT` to approved.
5. Certify the production-snapshot rehearsals with the reviewed production importer and source-to-target reconciliation end to end (two local clean-target rehearsals are already green).
6. Execute the legacy admission freeze for production, call/job drain, and the separately rehearsed traffic switch/rollback (runbook drafted; a development freeze rehearsal passed).
7. Approve the immutable release candidate (tested tree at baseline `cd30ef7b-955a-4a57-a2ab-231279637300`, currently uncommitted), production resources/configuration, operator window, rollback boundary, and switch-over. Keep `releaseCertified: false` until then.

## Evidence handling

Detailed reports, snapshots, credentials, recipient allowlists, and source/target hashes are retained in restricted local artifacts. Do not copy them into public CI reports or tracked files. Temporary cloud resources incur usage charges and need explicit retention/cleanup decisions after certification.
