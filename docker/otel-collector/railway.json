{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "/Dockerfile.otel-collector",
    "watchPatterns": ["docker/otel-collector/**", "Dockerfile.otel-collector"]
  },
  "deploy": {
    "restartPolicyType": "ON_FAILURE"
  }
}
