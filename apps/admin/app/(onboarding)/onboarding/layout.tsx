import { OnboardingRouteShell } from "@/components/onboarding-route-shell";

export default function OnboardingLayout({ children }: { children: React.ReactNode }) {
  return <OnboardingRouteShell>{children}</OnboardingRouteShell>;
}
