---
import LocalizedPage from "@/components/LocalizedPage.astro"
import { frenchPageByPath } from "@/lib/fr-pages"

const page = frenchPageByPath("/")
if (!page) throw new Error("Missing French homepage")
---

<LocalizedPage page={page} />
